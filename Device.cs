namespace pattern_bridge
{
    // Device
       // SonyBrand
       // SamsungBrand
    public interface Device
    {
         void TurnOn();
         void TurnOff();
         void SetChannel(int number);
    }
}